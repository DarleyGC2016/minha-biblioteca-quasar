export interface Todo {
  id: number;
  content: string;
}

export interface Livro {
  id: string;
  nome: string;
  anoPublicacao: string;
  autor: string;
  sinopse: string;
}

export interface Meta {
  totalCount: number;
}
